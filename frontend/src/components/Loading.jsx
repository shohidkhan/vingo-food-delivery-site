import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center items-center h-screen w-full bg-[#fddfd0]">
      <span className="loading loading-spinner loading-xl"></span>
    </div>
  );
};

export default Loading;
